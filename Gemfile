source "https://rubygems.org"

# Match GitHub Pages' dependency set so local builds mirror production.
gem "github-pages", group: :jekyll_plugins

# Ruby 3+ no longer ships WEBrick by default; add it for `jekyll serve`.
gem "webrick", "~> 1.8"
